import React from 'react';
import PropTypes from 'prop-types';

const Prueba = ({id,name}) => {
    return (
        <div>
                <h1>{id} - {name}</h1>
        </div>
    );
};

export default Prueba;
